'use client'; import { useState } from 'react'; import { useRouter } from 'next/navigation';
export default function ProductStatusToggle({id,active=true}:{id:number;active?:boolean}){const [value,setValue]=useState(active);const router=useRouter();async function toggle(){const next=!value;setValue(next);const r=await fetch(`/api/admin/products/${id}/status`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({isActive:next})});if(!r.ok){setValue(value);alert('Unable to update status');}else router.refresh();}return <button onClick={toggle} className={`rounded-full px-3 py-1 text-xs font-semibold ${value?'bg-green-100 text-green-700':'bg-gray-100 text-gray-500'}`}>{value?'Active':'Inactive'}</button>;}

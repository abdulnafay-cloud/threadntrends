'use client'; import { useState } from 'react'; export default function MessageStatus({id,initial}:{id:string;initial:string}){const [status,setStatus]=useState(initial);async function change(value:string){setStatus(value);await fetch('/api/admin/messages',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({id,status:value})});}return <select value={status} onChange={e=>change(e.target.value)} className="rounded border px-2 py-1 text-xs"><option value="new">New</option><option value="read">Read</option><option value="replied">Replied</option><option value="archived">Archived</option></select>;}

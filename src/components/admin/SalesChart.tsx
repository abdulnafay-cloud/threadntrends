'use client';
import { AreaChart, Area, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer } from 'recharts'; import { format, subDays } from 'date-fns';
interface SalesData{date:string;revenue:number;orders:number}
export default function SalesChart({data}:{data:SalesData[]}){const byDay=new Map(data.map(d=>[format(new Date(d.date),'yyyy-MM-dd'),d]));const chart=Array.from({length:7},(_,i)=>{const date=subDays(new Date(),6-i);const key=format(date,'yyyy-MM-dd');const d=byDay.get(key);return {label:format(date,'EEE'),revenue:d?.revenue||0,orders:d?.orders||0};});const total=chart.reduce((s,d)=>s+d.revenue,0);return <div className="rounded-2xl border border-black/5 bg-white p-5 shadow-sm"><div className="mb-4 flex items-end justify-between"><div><p className="text-xs font-semibold uppercase tracking-wider text-gray-500">Revenue</p><h3 className="mt-1 text-2xl font-bold">PKR {total.toLocaleString()}</h3></div><span className="rounded-full bg-green-50 px-3 py-1 text-xs font-semibold text-green-700">Last 7 days</span></div><div className="h-64"><ResponsiveContainer width="100%" height="100%"><AreaChart data={chart} margin={{top:8,right:8,left:0,bottom:0}}><defs><linearGradient id="revenueFill" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#a56a4b" stopOpacity={0.55}/><stop offset="100%" stopColor="#a56a4b" stopOpacity={0.04}/></linearGradient></defs><CartesianGrid stroke="#292421" strokeOpacity={0.08} vertical={false}/><XAxis dataKey="label" tickLine={false} axisLine={false} tick={{fontSize:11,fill:'#6f6964'}}/><YAxis tickLine={false} axisLine={false} tick={{fontSize:10,fill:'#6f6964'}} tickFormatter={v=>v>=1000?`₨${Math.round(v/1000)}k`:`₨${v}`}/><Tooltip contentStyle={{borderRadius:12,border:'1px solid #eee',boxShadow:'0 8px 24px rgba(0,0,0,.08)'}} formatter={(v)=>[`PKR ${Number(v).toLocaleString()}`,'Revenue']}/><Area type="monotone" dataKey="revenue" stroke="#292421" strokeWidth={2.5} fill="url(#revenueFill)" dot={{r:3,fill:'#a56a4b',stroke:'#292421',strokeWidth:2}} activeDot={{r:5}}/></AreaChart></ResponsiveContainer></div></div>}

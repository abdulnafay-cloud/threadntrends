'use client';
import { PieChart, Pie, Cell, Tooltip, ResponsiveContainer } from 'recharts';
const COLORS=['#b6ff39','#11110f','#6f8cff','#ff9b80','#d8d4ca'];
export default function OrderStatusChart({data}:{data:{status:string;count:number}[]}){const total=data.reduce((s,d)=>s+d.count,0);return <div className="rounded-2xl border border-black/5 bg-white p-5 shadow-sm"><div className="mb-2"><p className="text-xs font-semibold uppercase tracking-wider text-gray-500">Fulfilment</p><h3 className="mt-1 text-2xl font-bold">Order status</h3></div>{!total?<div className="flex h-64 items-center justify-center text-sm text-gray-400">No orders yet</div>:<div className="grid h-64 grid-cols-[1fr_1fr] items-center"><div className="h-full"><ResponsiveContainer width="100%" height="100%"><PieChart><Pie data={data} dataKey="count" nameKey="status" innerRadius={58} outerRadius={82} paddingAngle={3} stroke="none">{data.map((_,i)=><Cell key={i} fill={COLORS[i%COLORS.length]}/>)}</Pie><Tooltip formatter={(v,n)=>[`${v} orders`,String(n)]}/></PieChart></ResponsiveContainer></div><div className="space-y-3">{data.map((d,i)=><div key={d.status} className="flex items-center justify-between gap-3 text-sm"><span className="flex items-center gap-2 capitalize"><i className="h-2.5 w-2.5 rounded-full" style={{background:COLORS[i%COLORS.length]}}/>{d.status}</span><b>{d.count}</b></div>)}<div className="border-t pt-3 text-xs text-gray-500">{total} total orders</div></div></div>}</div>}

'use client'; export default function NewsletterExport({rows}:{rows:{email:string;status:string}[]}){function download(){const csv=['Email,Status',...rows.map(r=>`${r.email},${r.status}`)].join('\n');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv'}));a.download='newsletter-subscribers.csv';a.click();}return <button onClick={download} className="rounded-full border border-black/15 bg-white px-4 py-2 text-xs font-bold uppercase">Export CSV</button>;}

'use client';
import Link from 'next/link'; import { useMemo, useState } from 'react';
type Order={id:string;order_reference:string;customer_name:string;created_at:string|Date;total:number;status:string};
export default function OrdersTable({orders}:{orders:Order[]}){const [q,setQ]=useState('');const [status,setStatus]=useState('all');const filtered=useMemo(()=>orders.filter(o=>(status==='all'||o.status===status)&&(`${o.order_reference} ${o.customer_name}`.toLowerCase().includes(q.toLowerCase()))),[orders,q,status]);return <div><div className="mb-4 flex flex-wrap gap-3"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search order or customer" className="rounded-lg border px-3 py-2 text-sm"/><select value={status} onChange={e=>setStatus(e.target.value)} className="rounded-lg border px-3 py-2 text-sm"><option value="all">All statuses</option><option>confirmed</option><option>processing</option><option>shipped</option><option>delivered</option><option>cancelled</option></select><span className="self-center text-sm text-gray-500">{filtered.length} orders</span></div><div className="bg-white rounded-lg shadow overflow-x-auto"><table className="w-full"><thead><tr className="border-b"><th className="text-left p-3">Order #</th><th className="text-left p-3">Customer</th><th className="text-left p-3">Date</th><th className="text-right p-3">Total</th><th className="text-left p-3">Status</th><th className="text-center p-3">Action</th></tr></thead><tbody>{filtered.map(o=><tr key={o.id} className="border-b hover:bg-gray-50"><td className="p-3">{o.order_reference}</td><td className="p-3">{o.customer_name}</td><td className="p-3">{new Date(o.created_at).toLocaleDateString()}</td><td className="text-right p-3">PKR {Number(o.total).toLocaleString()}</td><td className="p-3"><span className="rounded bg-blue-100 px-2 py-1 text-xs">{o.status}</span></td><td className="text-center p-3"><Link href={`/admin/orders/${o.id}`} className="text-blue-600">View</Link></td></tr>)}</tbody></table>{!filtered.length&&<p className="p-6 text-center text-gray-500">No matching orders.</p>}</div></div>}

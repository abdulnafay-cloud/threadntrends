'use client';
import { useState } from 'react';
export default function DiscountManager() { const [form,setForm]=useState({code:'',percent:'',usageLimit:''}); const [saving,setSaving]=useState(false); async function submit(e:React.FormEvent){e.preventDefault();setSaving(true);const r=await fetch('/api/admin/discounts',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});if(r.ok) location.reload(); else alert('Unable to create discount');setSaving(false);} return <form onSubmit={submit} className="mb-6 flex flex-wrap gap-2 items-end bg-white p-4 rounded-lg shadow"><label className="text-sm">Code<input required value={form.code} onChange={e=>setForm({...form,code:e.target.value})} className="block border rounded px-3 py-2" /></label><label className="text-sm">Percent<input required type="number" min="1" max="100" value={form.percent} onChange={e=>setForm({...form,percent:e.target.value})} className="block border rounded px-3 py-2" /></label><label className="text-sm">Usage limit<input type="number" min="1" value={form.usageLimit} onChange={e=>setForm({...form,usageLimit:e.target.value})} className="block border rounded px-3 py-2" /></label><button disabled={saving} className="bg-blue-600 text-white rounded px-4 py-2">{saving?'Saving...':'Create code'}</button></form>; }

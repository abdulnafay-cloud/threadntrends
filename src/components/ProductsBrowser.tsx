'use client';
import { useSearchParams } from 'next/navigation';
import ProductCard from '@/components/ProductCard';
import { Suspense, useMemo, useState } from 'react';
import type { Product } from '@/lib/product-types';
function Browser({ products }: { products: Product[] }) { const params=useSearchParams(); const category=params.get('category')||''; const [count,setCount]=useState(12); const filtered=useMemo(()=>category?products.filter(p=>p.category.toLowerCase()===category.toLowerCase()):products,[products,category]); return <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12"><div className="flex flex-wrap justify-between items-end gap-6 mb-10"><div><div className="text-[10px] uppercase tracking-[0.18em] font-extrabold text-[#79776f] mb-2.5">Collection / 2026</div><h1 className="font-manrope font-bold text-[clamp(38px,5vw,72px)] leading-[0.95] tracking-[-0.055em]">{category||'All'} {category?'':'Products'}</h1></div><p className="max-w-[420px] text-[#79776f] text-sm leading-relaxed">Browse the latest Thread n Trends drops. Filter by category and discover pieces added by our store team.</p></div><div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-4 md:gap-7">{filtered.slice(0,count).map(p=><ProductCard key={p.id} product={p}/>)}</div>{count<filtered.length&&<div className="text-center mt-12"><button onClick={()=>setCount(c=>c+12)} className="bg-[#111] text-white px-8 py-3 rounded-full font-bold text-sm">Load more</button></div>}{filtered.length===0&&<div className="text-center py-20 text-[#79776f]">No products found in this category.</div>}</div>; }
export default function ProductsBrowser({ products }: { products: Product[] }) { return <Suspense fallback={<div className="min-h-[50vh]"/>}><Browser products={products}/></Suspense>; }

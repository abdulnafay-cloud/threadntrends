import { requireAdmin } from '@/lib/admin-auth';
import { database, ensureAuthSchema } from '@/lib/db';
import DiscountManager from '@/components/admin/DiscountManager';
export default async function DiscountsPage() { await requireAdmin(); await ensureAuthSchema(); const { rows } = await database.query(`SELECT * FROM tnt_discounts ORDER BY created_at DESC`); return <section><h1 className="text-2xl font-semibold mb-6">Discounts</h1><div className="bg-white rounded-lg shadow overflow-x-auto"><table className="w-full text-sm"><thead><tr className="border-b"><th className="p-3 text-left">Code</th><th className="p-3 text-left">Discount</th><th className="p-3 text-left">Uses</th><th className="p-3 text-left">Status</th></tr></thead><tbody>{rows.map((d) => <tr key={d.id} className="border-b"><td className="p-3 font-medium">{d.code}</td><td className="p-3">{d.percent}%</td><td className="p-3">{d.used_count}{d.usage_limit ? ` / ${d.usage_limit}` : ''}</td><td className="p-3">{d.is_active ? 'Active' : 'Inactive'}</td></tr>)}</tbody></table>{rows.length === 0 && <p className="p-4 text-gray-500">No discount codes yet.</p>}</div></section>; }
